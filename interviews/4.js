/**
 * FB Contract
 * Given an array of numbers, move all the zeros to the back of the array in place and
 * return the length of the nonzero part of the array
 * @param {array} a
 * @returns {number}
 */

const solution = (a) => {
  return 0;
};

module.exports = {
  solution,
};
