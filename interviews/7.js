/** Write a function that takes in a rotated sorted array and return the smallest number
 * @param {array}
 * @returns {number}
 */

const solution = (a) => {
  return 0
}

module.exports = {
  solution
}
