/**
 * FB Onsite, Addepar Onsite
 * Given an array of task duration, find the minimum time
 * it takes for all the tasks to complete if you had 3 machines
 * @param {array} a
 * @returns {number}
 */

const solution = (a) => {
  return 0;
};

module.exports = {
  solution,
};
