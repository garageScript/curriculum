/* Minimum Coins
for Change
Given an array of coin values and a target number
of change to create with any combination of the coin values,
find the minimum number of coins to make up the value of change.
There will always be an infinite number of 1 cent coin available.

solution([], 40) // return 40

solution([4, 5], 8) // return 2
*/

module.exports = {
    solution,
};
