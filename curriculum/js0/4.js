/**
 * Takes in 2 numbers, returns the larger number
 * @param {number} a
 * @returns {number}
 */

const solution = (a, b) => {
  return 0;
  if (a > b) {
    return a;
  } else {
    return b;
  };
};
module.exports = {
  solution,
};
