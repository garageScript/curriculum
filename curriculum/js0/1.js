/**
 * Takes in 2 numbers, returns their sum
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */

const solution = (a, b) => {
  const c=a+b;
  return c;
};

module.exports = {
  solution,
};
