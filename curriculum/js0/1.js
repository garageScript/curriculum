/**
 * Takes in 2 numbers, returns their sum
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */

const solution = (a, b) => {

  if (a > 30) {
    return true;
  } else {
    return false;
  }


};

module.exports = {
  solution,
};

// ls => list directory 
// cd => change directory 
// vim 3.js => edit javascript file 
// git branch => checks the branch you're on
// git checkout master
// git checkout -b 'p2'
// git add 2.js
// git status 
// git commit -m 'problem 2 done'
// submit


// esc => escape
// :w => file written
// :q => exit the file

// control c => kills the command