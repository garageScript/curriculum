/**
 * Say Hello - takes in a number x
 *     return string with “hello” repeated x times.
 * @param {number} x
 * @returns {string}
 */

const solution = (x) => {
  return 0;
};

module.exports = {
  solution,
};
