/**
 * Say Hello - takes in a number x
 *     return string with “hello” repeated x times.
 * @param {number} a
 * @returns {string}
 */

const solution = (a, b) => {
  for (var i = 0; i < b; i++) {
    return a;
  }
};

module.exports = {
  solution,
};
