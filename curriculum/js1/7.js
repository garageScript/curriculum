/**
 * isPrime - returns if number is prime
 *    Prime: numbers can only be divided by 1 and itself
 * @param {number} a
 * @returns {boolean}
 */

const solution = (a) => {
    const isPrime = (num) => {
        for(let i=0;i<num; )
    }
    return isPrime(a);
};

module.exports = {
  solution,
};
