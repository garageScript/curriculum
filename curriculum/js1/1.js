/**
 * Call X - takes in 2 numbers, return their sum
 * @param {number} a
 * @param {number} b
 * @returns {boolean}
 */

const solution = (a, b, result) => {
  result=a+b;
  return result;
};

module.exports = {
  solution,
};
