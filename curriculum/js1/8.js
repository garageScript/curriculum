/**
 * Greatest Common Divisor - returns greatest common divisor
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */

const solution = (a, b) => {
  return 0;
};

module.exports = {
  solution,
};
