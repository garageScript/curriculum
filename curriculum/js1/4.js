/**
 * Print String - takes in a number n and string s,
 *     return the string repeated n number of times.
 * @param {number} n
 * @param {string} s
 * @returns {string}
 */

const solution = (n, s) => {
  return 0;
};

module.exports = {
  solution,
};
