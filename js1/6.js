/**
 * Write a function called solution that
 *   takes in a number
 *   and returns true if the number is a prime number
 *   false otherwise
 * @param {number} a
 * @returns {boolean}
 */

const solution = (a) => {
  return true
}

module.exports = {
  solution
}
