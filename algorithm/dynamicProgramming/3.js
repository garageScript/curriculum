/**
 * Given an array of coins and a value, find the minimum number
 * of coins required to make up the value
 * @param {array} a
 * @param {number} v
 * @returns {number}
 */

const solution = (a, v) => {
  return 0;
};

module.exports = {
  solution,
};
