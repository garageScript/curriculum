/**
 * Given an array of weights and a maximum capacity, find the maximum
 * weight we can achieve.
 * @param {array} a
 * @param {number} v
 * @returns {number}
 */

const solution = (a, v) => {
  return 0;
};

module.exports = {
  solution,
};
