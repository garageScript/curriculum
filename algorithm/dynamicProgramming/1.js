/**
 * Given an array of house values, find the maximum loot you can get.
 * If you steal from house n, you can't steal from its neighbors: n-1 and n+1
 * @param {array} a
 * @returns {number}
 */

const solution = (a) => {
  return 0;
};

module.exports = {
  solution,
};
