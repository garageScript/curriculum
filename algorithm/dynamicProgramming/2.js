/**
 * Given an array of numbers, find the longest increasing nonconsecutive sequence
 * @param {array} a
 * @returns {number}
 */

const solution = (a) => {
  return 0;
};

module.exports = {
  solution,
};
