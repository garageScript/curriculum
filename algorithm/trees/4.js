/**
 * Given a node {val: 5}, return value of the sum of values of biggest subtree
 * @param {node} a
 * @returns {number}
 */

const solution = (a) => {
  return 0;
};

module.exports = {
  solution,
};
