/**
 * Each employee has an array of employees if he/she is a manager.
 * Each employee has a fun value.
 * If an employee goes to a party, his direct manager cannot go to party.
 * Given an employee, find the max fun value of a party
 * @param {node} a
 * @returns {number}
 */

const solution = (a) => {
  return 0;
};

module.exports = {
  solution,
};
