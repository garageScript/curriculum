/**
 * Write a function called solution that
 * Takes in 2 numbers, return if their sum is greater than 10
 * @param {number} a
 * @param {number} b
 * @returns {boolean}
 */

const solution = (a, b) => {
  return true
}

module.exports = {
  solution
}
