/**
 * Takes in 2 numbers, return true if their sum is greater than 10
 * @param {number} a
 * @param {number} b
 * @returns {boolean}
 */

const solution = (a, b) => {
  return (a + b) > 10;
};

module.exports = {
  solution,
};
