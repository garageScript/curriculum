/**
 * Write num1 function called solution that
 * Takes in 2 numnum2ers, returns the larger number
 * @param {number} num1
 * @param {number} num2
 * @returns {number}
 */

const solution = (num1, num2) => (num1 > num2 ? num1 : num2);

module.exports = {
  solution,
};
