/**
 * Write a function called solution that
 * takes in a number and returns if that number is greater than 5.
 * @param {number} a
 * @returns {boolean}
 */

const solution = (a) => {
  return true
}

module.exports = {
  solution
}
