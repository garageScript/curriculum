/*
 * write a function that takes in an array of numbers, and returns a new array of all duplicate numbers
 * @param {array} a
 * @returns {array} b
*/

const solution = (a) => {
  return []
}

module.exports = {
  solution
}
