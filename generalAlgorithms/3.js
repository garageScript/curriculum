/**
 * FB Contract
 * Given an array, return an array where each element is the product of
 * all numbers except the number at that index in the original array
 * @param {array} a
 * @returns {array}
 */

const solution = (a) => {
  return []
};

module.exports = {
  solution,
};
