/**
 * Write a function that takes in an array of integers and a target value
 * and returns true / false if there exists a contiguous subarray
 * that sums to the target value
 * @param {array} a
 * @param {number} target
 * @returns {boolean}
 */

const solution = (a, target) => {
  return true;
};

module.exports = {
  solution,
};
