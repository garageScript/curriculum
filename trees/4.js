/**
 * Given a binary tree and a node, find the distance to that node.
 * @param {node} t
 * @param {node} a
 * @returns {number}
 */
const solution = (t, n) => {
  return 0;
};

module.exports = {
  solution,
};
