/* SLEEP SORT

Given an array an a
function (f1), call f1 with the sorted array.You are not allowed to use sort, you can only use setTimeout.

sleepSort([9, 8, 1, 5], console.log) // will print out [1,5,8,9]
*/

const solution = (arr, cb) => {
    return cb([]);
};

module.exports = solution;
