/* parseJSON
Given a JSON string of an object,
return the parsed object.Ignore arrays and you can assume keys will always be
alpha numeric(numbers and letters)
*/

const parseJSON = (str) => {
    return {};
};

module.exports = parseJSON;